<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="stylesheet" href="style.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background: #fafafa;
        }

        h2 {
            color: red; /* tiêu đề màu đỏ */
            margin: 20px 0 10px 0;
            text-align: center;
        }

        .container {
            width: 960px;
            margin: 0 auto;
            border: 1px solid #333;
            background: #fff;
        }

        header {
            background: #ddd;
            text-align: center;
            padding: 20px;
            font-weight: bold;
            border-bottom: 1px solid #aaa;
        }

        .main {
            display: flex;
            min-height: 300px;
        }

        nav {
            width: 200px; /* menu cố định */
            background: #f9f9f9;
            padding: 15px;
            border-right: 1px solid #aaa;
        }

            nav h3 {
                margin-bottom: 10px;
            }

            nav ul {
                list-style: none;
            }

            nav li {
                padding: 5px 0;
                border-bottom: 1px dashed #ccc;
            }

        .content {
            flex: 1;
            padding: 15px;
            display: flex;
            gap: 10px;

        }

        .left {
            flex: 2; /* ô chính lớn hơn */
            padding: 15px;
            background: #fff;
            text-align: center;
            font-weight: bold;
            border: 1px solid #000000;
        }

        .right {
            flex: 1; /* ô nhỏ hơn */
            display: flex;
            flex-direction: column;
            gap: 10px;
            border: 1px solid #000000;
        }

            .right .box {
                flex: 1;
                padding: 15px;
                background: #fff;
                text-align: center;
                font-weight: bold;
                border: 1px solid #000000;
            }
  </style>

</head>
<body>
    <h2>Bài 1: Layout bằng HTML + CSS</h2>
    <div class="container">
        <header>HEADER / LOGO</header>
        <div class="main">
            <nav>
                <h3>Menu</h3>
                <ul>
                    <li>Trang chủ</li>
                    <li>Giới thiệu</li>
                    <li>Dịch vụ</li>
                    <li>Liên hệ</li>
                </ul>
            </nav>
            <div class="content">
                <div class="left">Nội dung chính</div>
                <div class="right">
                    <div class=" content">
                        <div class="box">Nội dung phụ 2</div>
                    </div>
                    <div class=" content">
                        <div class="box">Nội dung phụ 3</div>
                    </div>
                </div>
        </div>
    </div>
    <div>
        @RenderBody()
    </div>
</body>
</html>
